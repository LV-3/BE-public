# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle

name: Java CI with Gradle

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - uses: actions/checkout@v3
<<<<<<< HEAD
      - name: application.yml 생성
        run: |
          touch ./src/main/resources/application.yml
          echo "${{ secrets.APPLICATION }}" > ./src/main/resources/application.yml
          cat ./src/main/resources/application.yml
      
      - name: gradlew 권한 변경
        run: chmod +x ./gradlew
=======
      - run: touch ./src/main/resources/application.yml
      - run: echo "${{ secrets.MYSQL_URL }}" >> ./src/main/resources/application.yml
      - run: echo "${{ secrets.MYSQL_USERNAME }}" >> ./src/main/resources/application.yml
      - run: echo "${{ secrets.MYSQL_PASSWORD }}" >> ./src/main/resources/application.yml
      - run: echo "${{ secrets.MONGODB_URI }}" >> ./src/main/resources/application.yml
      - run: echo "${{ secrets.KAFKA_SERVER }}" >> ./src/main/resources/application.yml
      - run: cat ./src/main/resources/application.yml

      - run : ./gradlew clean build --exclude-task test
>>>>>>> 1d613ed7bf022c7a9e1c763b27f7a81f79a4b467

      - name: Build with Gradle
        uses: gradle/gradle-build-action@67421db6bd0bf253fb4bd25b31ebb98943c375e1
        with:
          arguments: build


  
